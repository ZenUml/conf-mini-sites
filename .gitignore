node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.*
# …but DO track the committed E2E template (names only, empty values) — it documents the suite's env.
!tests/e2e/.env.example
*.log
.DS_Store

# Nested workspace-repo worktrees (a different git repo) live under .claude/worktrees — not part of this repo.
.claude/worktrees/

# JetBrains IDE
.idea/
forge-app/ui-src/.app.compiled.css

# Storybook build output
forge-app/storybook-static/

# Playwright e2e
tests/e2e/.auth/
test-results/
playwright-report/

# demo-pipeline (agent-first demo video PoC) — generated run artifacts, the project-local Python venv for
# the pinned Kokoro TTS adapter, and the cross-run narration cache (WAV files + sidecar manifests, keyed by a
# hash of provider/model/voice/text — see demo-pipeline/src/narration.ts). None of these are committed.
.demo-runs/
demo-pipeline/.venv/
demo-pipeline/.demo-cache/
demo-pipeline/voice/__pycache__/

# promo video render artifacts (large media; regenerate with pnpm promo:*)
promo/out/
promo/capture/.backdrop.png
promo/capture/.logo.svg
promo/tmp/
