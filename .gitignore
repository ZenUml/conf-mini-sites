node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.*
# …but DO track the committed E2E template (names only, empty values) — it documents the suite's env.
!tests/e2e/.env.example
*.log
.DS_Store

# Nested workspace-repo worktrees (a different git repo) live under .claude/worktrees — not part of this repo.
.claude/worktrees/

# JetBrains IDE
.idea/
forge-app/ui-src/.app.compiled.css

# Storybook build output
forge-app/storybook-static/

# Playwright e2e
tests/e2e/.auth/
test-results/
playwright-report/

# demo-pipeline (agent-first demo video PoC) — generated run artifacts and the
# project-local Python venv for the pinned Kokoro TTS adapter. Never committed.
.demo-runs/
demo-pipeline/.venv/
