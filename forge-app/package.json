{
  "name": "conf-mini-sites-forge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Conf Mini-Sites — Forge app shell (macro + resolver + Custom UI). Backend Workers live at the repo root.",
  "scripts": {
    "build:ui": "esbuild static/view/src/app.js --bundle --outfile=static/view/main.js --format=iife --minify"
  },
  "dependencies": {
    "@forge/api": "^7.2.2",
    "@forge/resolver": "^1.8.0"
  },
  "devDependencies": {
    "@forge/bridge": "^5.17.0",
    "esbuild": "^0.28.1"
  }
}
